import { AnimImgLoadDirective } from './anim-img-load.directive';

describe('AnimImgLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimImgLoadDirective();
    expect(directive).toBeTruthy();
  });
});
